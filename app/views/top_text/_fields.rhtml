<%= f.tr_text_field :name, :label => 'Name', :req => true, :class => 'ucase' %>
<%= f.tr_text_field :path, :label => 'Path', :req => true, :help => 'Display text on pages that match this path. Use % at the end as a wildcard character.' %>
<%= f.tr_text_area :text, :label => 'Text', :req => true, :help => 'The text to display at the top of the page.' %>