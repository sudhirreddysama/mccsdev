<%= f.tr_text_field :name, :label => 'Name', :req => true %>
<%= f.tr_text_field :emails, :label => 'Emails', :help => 'Separate multiple emails with a comma.' %>
<%= f.tr_select :personnel_area_id, PersonnelArea.find(:all, :order => 'personnel_areas.name').map { |d| [d.name, d.id] }, :include_blank => true, :label => 'Personnel Area', :req => true %>