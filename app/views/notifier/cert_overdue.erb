A certified list for your agency/department is due <%= @lbl.upcase %>. Please enter actions for each candidate and "complete" the list by clicking the green bar at the top of the cert overview page (link below).

Certified List Title:
<%= @c.title %>

View Certified List:
https://cs.monroecounty.gov/mccs<%= RAILS_ENV == 'development' ? 'dev' : '' %>/cert/view/<%= @c.id %>

This is an AUTOMATED email from HR MAX being sent to the agency/department users and the HR liaison for the certified list.